node_modules/
dist/
dist-tests/
coverage/
*.mcpb

# Local secrets / dev env
.env
.env.*
!.env.example

# Signing material
*.pem
*.key

# Logs / OS
*.log
.DS_Store

# Personal/local Claude Code settings (permission grants can embed secrets
# used in allowlisted commands) — .claude/skills is a tracked symlink, this
# is not.
.claude/settings.local.json
