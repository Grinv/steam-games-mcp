// Kept in sync with package.json by a unit test (see __tests__/version.test.ts).
export const VERSION = "0.8.1";

export const USER_AGENT = `steam-games-mcp/${VERSION} (+https://github.com/Grinv/steam-games-mcp)`;
